var {When} = require('@cucumber/cucumber');

const actions         = require('../../util/action/actions'),
      selectorHelper  = require('../../util/common/selectorHelper'),
      checks          = require('../../util/check/checks.js');

When(/^I enter the username on the page$/, function() {

});
